# EDA AI 智能助手 — 依赖清单
# 安装方式: pip install -r requirements.txt

# ── GUI 框架 ──
PyQt5>=5.15.0

# ── 数据处理 ──
pandas>=2.0.0
openpyxl>=3.1.0          # Excel 读写
xlrd>=2.0.0              # 旧版 .xls 读取

# ── AI / LLM ──
requests>=2.28.0         # HTTP 请求（LLM API，兼容 OpenAI/DeepSeek/通义千问/智谱/Kimi 等）
sseclient-py>=1.8.0      # SSE 流式响应（可选）

# ── HTML BOM 生成 ──
jinja2>=3.1.0

# ── 工具 ──
python-dotenv>=1.0.0     # 环境变量管理

# ── 打包发布 ──
pyinstaller>=5.0.0       # 生成 Windows 可执行文件

# ── 开发/测试 ──
pytest>=7.0.0
pytest-qt>=4.2.0         # PyQt5 测试支持
