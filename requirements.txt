# EDA AI 智能助手 — 依赖清单
# 安装方式: pip install -r requirements.txt

# ── GUI 框架 (Web UI) ──
eel>=0.16.0

# ── 数据处理 ──
pandas>=2.0.0
openpyxl>=3.1.0          # Excel 读写
xlrd>=2.0.0              # 旧版 .xls 读取

# ── AI / LLM ──
requests>=2.28.0         # HTTP 请求（LLM API，兼容 OpenAI/DeepSeek/通义千问/智谱/Kimi 等）
sseclient-py>=1.8.0      # SSE 流式响应（可选）
anthropic>=0.30.0        # Anthropic Claude API (Messages API)

# ── RAG 知识库 ──
chromadb>=0.4.0          # 向量数据库 (RAG 检索增强生成)

# ── API 服务器 (LCEDA 插件后端) ──
fastapi>=0.110.0         # REST API 框架
uvicorn[standard]>=0.27.0  # ASGI 服务器
sse-starlette>=1.8.0     # SSE (Server-Sent Events) 流式推送

# ── HTML BOM 生成 ──
jinja2>=3.1.0

# ── 工具 ──
python-dotenv>=1.0.0     # 环境变量管理

# ── 打包发布 ──
pyinstaller>=5.0.0       # 生成 Windows 可执行文件

# ── 文件监听 ──
watchdog>=3.0.0

# ── 系统交互 ──
pynput>=1.7.0            # 全局快捷键
pystray>=0.19.0          # 系统托盘
Pillow>=9.0.0            # 托盘图标生成 (pystray 依赖)

# ── 开发/测试 ──
pytest>=7.0.0
pytest-cov>=4.0.0        # 测试覆盖率
